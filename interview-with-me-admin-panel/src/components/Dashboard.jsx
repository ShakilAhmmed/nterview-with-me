import Layout from "./Layout";

const Dashboard = () => {
    const renderElement = () => {
            return <Layout></Layout>
    }

    return (
        <>

            {
                renderElement()
            }

        </>
    );
};


export default Dashboard;
